# Бэклог

## Рефакторинг

### study.py
- Рефакторинг `core/nirs/study.py` — самый крупный модуль, требует разбора

### nirs/ -> core/
- Вынести `study.py` из `core/nirs/` в `core/` (например `core/event_study_runner.py`)
- `nirs` — название курсовой, а не функциональности
- После выноса удалить пакет `core/nirs/`

### Дублирование event study
- `event_impact_analyzer.py` (старый, с CPI) и `nirs/study.py` (новый, 3 модели) делают одно и то же
- Решить: объединить или оставить оба

### Относительные пути к данным
- `stock_data_provider.py`: `STOCKS_FOLDER = "../data/stocks"` — сломается не из notebooks/
- `cpi_data_provider.py`: `../data/stats/...` — та же проблема
- Перевести на `os.path.dirname(__file__)` как в `dividend_data_provider.py`

### stock_data_provider.py
- `load_stock_data`, `load_normalized_stock` — внутренние функции, пометить как приватные (`_`)

---

## Ноутбук 22 (интерактивный event study)

### Агрегированный анализ
- Средний CAR по всем событиям тикера — одна кривая
- t-stat / p-value на агрегированном уровне

### Параметры
- Отступ оценочного окна (gap между оценочным и событийным)
- Экспорт данных графика в CSV

### Данные
- Почасовая / поминутная динамика (дневных данных для дивидендов может быть мало)
- Источник событий из `data/db/events.csv` (общая база с тэгами)
- Импортировать даты дивидендов в БД событий + добавить тэги

---

## Модели и аналитика

### Калибровка оценочного периода
- Фильтрация выбросов в оценочном окне перед fit (например, ±3σ)
- Длинный оценочный период накапливает выбросы — модель должна обучаться на нормальном поведении

### Автоматический поиск аномалий (P1, киллер-фича)
- Три уровня: одна акция, сопоставление с другими активами, поиск неизвестных событий
- Пример 1: CAR вышел за 95% ДИ + объём вырос в 2 раза → значимая аномалия
- Пример 2: сканируем весь ряд котировок, находим дни с аномальной доходностью без известного события → «что тут произошло?»
- Подробнее: `docs/ANOMALY_DETECTION.md`

---

## Визуализация

### Стакинг графиков
- Возможность ставить несколько графиков друг под другом для сравнения
- Например: график акции + индекс Мосбиржи + другой актив
- Требует рефакторинга кода построения графиков

### Расширенное окно на графике
- Показывать не только событийное окно, но и контекст до/после — чтобы видеть всю картину

### Лассо (концепт)
- Выделение области на графике мышкой → показ статистики по выделенному участку (CAR, объём, даты)
