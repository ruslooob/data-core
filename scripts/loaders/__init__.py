"""Загрузчики данных из внешних источников.

Каждый модуль соответствует одному источнику данных и определяет
до двух стадий:

- `fetch()` — ходит во внешний API/сайт, кладёт snapshot в `data/`.
- `load(pg)` — читает локальные файлы и льёт строки в Postgres
  через `INSERT ... ON CONFLICT DO NOTHING` (идемпотентно).

Стадии могут отсутствовать: у `stock_splits` и `ruonia` сейчас
нет fetch — файлы лежат вручную. Оркестратор `scripts/load_all_data.py`
пропускает отсутствующие стадии.

Принципы (раскрытие см. в docs/SPEC_LOADERS.md):
- двухступенчатая архитектура (источник → файл → БД)
- идемпотентность на обоих уровнях
- инкрементальная загрузка (БД — источник правды о том, что уже есть)
"""
